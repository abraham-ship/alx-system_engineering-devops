# configure Nginx to proxy HTTP requests of route to a Gunicorn instance listening on port 5002

server {
        listen 80;
        listen [::]:80;
        add_header X-Served-By $hostname;

        location /api/ {
                proxy_pass http://127.0.0.1:5002;
        }
}
